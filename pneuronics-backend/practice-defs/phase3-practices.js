const phaseId = '6a358e67fc29b5a4714447b2';

const m12='6a358e67fc29b5a4714447b5', m13='6a358e68fc29b5a4714447df';

const l12_1='6a4e93cfe1d6a2ad969330f2';
const l13_1='6a4e985fc3dddb5f97fcad44';

function set(moduleId, lessonId, items) {
  return items.map((it, i) => ({ phaseId, moduleId, lessonId, order: i + 1, ...it }));
}

module.exports = [
  ...set(m12, l12_1, [
    { difficulty: 'beginner', title: "The Walrus Operator and match-case: Assigning Inside Expressions, Pattern-Matching Values",
      titleKn: "Walrus Operator ಮತ್ತು match-case: Expressions ಒಳಗೆ Assign ಮಾಡುವುದೂ, Values Pattern-Match ಮಾಡುವುದೂ",
      problem: "The walrus operator `:=` lets you assign a value to a variable AS PART OF a larger expression (e.g. `if (n := len(data)) > 10:` both computes `len(data)` and stores it in `n` in one line), while `match`/`case` (Python's structural pattern matching) lets you compare a value against multiple possible patterns more expressively than a long if/elif chain.\n\n1) Explain concretely why `if (n := len(data)) > 10: print(n)` is genuinely more concise than the equivalent `n = len(data)` followed by `if n > 10: print(n)` on a separate line -- what specific repetition or extra line does the walrus operator eliminate, and in what kind of situation (e.g. inside a `while` loop condition) does this become genuinely more valuable than just a style preference?\n\n2) Explain the basic structure of a `match` statement -- given `match command: case 'start': ... case 'stop': ... case _: ...`, what does the `case _:` pattern genuinely mean, and why is it commonly included as the LAST case (similar to a final `else` in an if/elif chain)?",
      problemKn: "walrus operator `:=` ನಿಮಗೆ ಒಂದು ಮೌಲ್ಯವನ್ನೂ ಒಂದು ದೊಡ್ಡ expression ya ಭಾಗವಾಗಿ ಒಂದು variable ಗೆ assign ಮಾಡಲು ಅನುಮತಿಸುತ್ತದೆ, ಆದರೆ `match`/`case` (Python ya structural pattern matching) ನಿಮಗೆ ಒಂದು ಮೌಲ್ಯವನ್ನೂ ಬಹು ಸಂಭಾವ್ಯ patterns ವಿರುದ್ಧ ಹೋಲಿಸಲು ಅನುಮತಿಸುತ್ತದೆ.\n\n1) `if (n := len(data)) > 10:` ಏಕೆ ನಿಜವಾಗಿ ಸಂಕ್ಷಿಪ್ತ ಎಂದು ವಿವರಿಸಿ.\n\n2) `match` statement ya ಮೂಲಭೂತ ರಚನೆಯನ್ನೂ ವಿವರಿಸಿ, `case _:` ಪ್ಯಾಟರ್ನ್ ya ಅರ್ಥ ಸೇರಿಸಿ." },
    { difficulty: 'intermediate', title: 'Genuinely Rewriting Input Validation Using the Walrus Operator and Type Hints',
      titleKn: 'Walrus Operator ಮತ್ತು Type Hints ಬಳಸಿ Input Validation ಅನ್ನೂ ನಿಜವಾಗಿ ಮರುಬರೆಯುವುದೂ',
      problem: "Genuinely rewrite a piece of repetitive code using the walrus operator, and genuinely add type hints to a function, verifying both changes preserve correct behavior.\n\n1) Genuinely write a `while` loop that repeatedly calls `input('Enter a positive number (or -1 to stop): ')`, converts to int, and processes it, WITHOUT the walrus operator first (calling input twice -- once before the loop, once at the end of the loop body, the classic 'priming read' pattern) -- run it and confirm it works.\n\n2) Genuinely rewrite the SAME loop using the walrus operator (`while (n := int(input(...))) != -1:`) to eliminate the duplicate input call, run it with the SAME test inputs, and confirm it produces IDENTICAL behavior with less code.\n\n3) Genuinely write a function `def calculate_area(width: float, height: float) -> float:` with type hints, call it with genuinely valid float arguments and confirm it works normally, then genuinely call it with a STRING argument (e.g. `calculate_area('5', 3.0)`) and report whether Python actually PREVENTS this call or lets it run anyway with a runtime error/wrong result -- explain, based on what you observe, why type hints are documentation/tooling aids rather than enforced runtime constraints in standard Python.",
      problemKn: "ಒಂದು ಪುನರಾವರ್ತಿತ code ಅನ್ನೂ walrus operator ಬಳಸಿ ಮರುಬರೆಯಿರಿ, ಮತ್ತು ಒಂದು function ಗೆ type hints ಸೇರಿಸಿ, ಎರಡೂ ಬದಲಾವಣೆಗಳು ಸರಿಯಾದ ವರ್ತನೆ ಉಳಿಸಿಕೊಳ್ಳುತ್ತವೆ ಎಂದು ಪರಿಶೀಲಿಸಿ.\n\n1) walrus operator ಇಲ್ಲದೆ ಒಂದು `while` loop ಬರೆಯಿರಿ (classic 'priming read' pattern).\n\n2) ಅದೇ loop ಅನ್ನೂ walrus operator ಬಳಸಿ ಮರುಬರೆಯಿರಿ ಮತ್ತು ಒಂದೇ ಫಲಿತಾಂಶ ಪಡೆಯುತ್ತದೆ ಎಂದು ಖಚಿತಪಡಿಸಿ.\n\n3) type hints ಜೊತೆ ಒಂದು function ಬರೆಯಿರಿ ಮತ್ತು ಇದು ತಪ್ಪಾದ type ಗಳನ್ನೂ ನಿಜವಾಗಿ ತಡೆಯುತ್ತದೆಯೇ ಎಂದು ಪರೀಕ್ಷಿಸಿ ವರದಿ ಮಾಡಿ." },
    { difficulty: 'advanced', title: 'Genuinely Building a Command Dispatcher Using match-case and Structured Exception Handling',
      titleKn: 'match-case ಮತ್ತು Structured Exception Handling ಬಳಸಿ ಒಂದು Command Dispatcher ಅನ್ನೂ ನಿಜವಾಗಿ ನಿರ್ಮಿಸುವುದೂ',
      problem: "Genuinely build a small command-line dispatcher combining match-case for control flow and a structured try/except/else/finally block for genuine error handling.\n\n1) Genuinely write a function that takes a command string and uses `match command.split():` to handle patterns like `['add', x, y]` (compute and return x+y), `['echo', *rest]` (join and return the rest), and `case _:` (return an 'unknown command' message) -- genuinely test it with at least 4 different command strings, including one that matches each pattern and one that hits the fallback case, reporting the actual output for each.\n\n2) Genuinely wrap the numeric conversion (`int(x)`, `int(y)` for the 'add' command) in a try/except that catches `ValueError` specifically (for when x or y isn't a valid number) and returns a clear error message instead of crashing -- test this by deliberately passing a non-numeric value to the 'add' command and reporting the actual error message your handler produced.\n\n3) Genuinely add an `else` clause (runs only if no exception occurred) that logs 'command succeeded' and a `finally` clause (always runs) that logs 'command processing complete' -- test both the success path and the error path, reporting the exact sequence of printed messages for each, and explain why `finally` running in BOTH cases (success and failure) makes it genuinely useful for cleanup code that must always execute regardless of whether an error happened.",
      problemKn: "match-case ಅನ್ನೂ control flow ಗಾಗಿ ಮತ್ತು ಒಂದು structured try/except/else/finally block ಅನ್ನೂ ನಿಜ error handling ಗಾಗಿ ಸಂಯೋಜಿಸುವ ಒಂದು ಚಿಕ್ಕ command-line dispatcher ಅನ್ನೂ ನಿಜವಾಗಿ ನಿರ್ಮಿಸಿ.\n\n1) `match command.split():` ಬಳಸಿ ವಿಭಿನ್ನ patterns ನಿಭಾಯಿಸುವ ಒಂದು function ಬರೆಯಿರಿ, ಕನಿಷ್ಠ 4 ವಿಭಿನ್ನ command strings ಜೊತೆ ಪರೀಕ್ಷಿಸಿ.\n\n2) numeric conversion ಅನ್ನೂ try/except ನಲ್ಲಿ wrap ಮಾಡಿ, ValueError ಅನ್ನೂ ನಿರ್ದಿಷ್ಟವಾಗಿ ಹಿಡಿಯಿರಿ, ಪರೀಕ್ಷಿಸಿ.\n\n3) ಒಂದು `else` clause ಮತ್ತು ಒಂದು `finally` clause ಸೇರಿಸಿ, success ಮತ್ತು error paths ಎರಡನ್ನೂ ಪರೀಕ್ಷಿಸಿ, ನಿಖರ message ಅನುಕ್ರಮ ವರದಿ ಮಾಡಿ." },
  ]),
  ...set(m13, l13_1, [
    { difficulty: 'beginner', title: "Virtual Environments and pip freeze: Isolating Each Project's Dependencies",
      titleKn: "Virtual Environments ಮತ್ತು pip freeze: ಪ್ರತಿ Project ya Dependencies ಅನ್ನೂ Isolate ಮಾಡುವುದೂ",
      problem: "A virtual environment (created via `python -m venv myenv`) is an isolated Python installation with its OWN separate set of installed packages, letting different projects on the same computer use different (even conflicting) package versions without interfering with each other -- `pip freeze > requirements.txt` captures the EXACT versions of every package currently installed in the active environment.\n\n1) Explain concretely why two different projects needing DIFFERENT versions of the same package (e.g. Project A needs `requests==2.25.0` and Project B needs `requests==2.31.0`) would genuinely conflict if you installed packages globally (system-wide) rather than using separate virtual environments for each project -- what would happen to Project A if you installed `requests==2.31.0` globally to satisfy Project B?\n\n2) Explain why running `pip freeze > requirements.txt` and committing that file to a project is genuinely more useful than just telling a collaborator 'install requests, flask, and numpy' verbally -- what specific problem (connecting to what you learned about pip version-pinning) does capturing EXACT versions in a file solve that a vague verbal list does not.",
      problemKn: "ಒಂದು virtual environment (`python -m venv myenv` ಮೂಲಕ ರಚಿಸಿದ) ಒಂದು ಪ್ರತ್ಯೇಕಿತ Python installation ಆಗಿದ್ದು, ಇದು ತನ್ನದೇ ಆದ ಪ್ರತ್ಯೇಕ installed packages ಸೆಟ್ ಹೊಂದಿದೆ, ಒಂದೇ ಕಂಪ್ಯೂಟರ್ ನಲ್ಲಿ ವಿಭಿನ್ನ projects ಗೆ ವಿಭಿನ್ನ package versions ಬಳಸಲು ಅನುಮತಿಸುತ್ತದೆ.\n\n1) ಎರಡು ವಿಭಿನ್ನ projects ಗೆ ಅದೇ package ya ವಿಭಿನ್ನ versions ಬೇಕಾದರೆ ಏಕೆ ಘರ್ಷಣೆ ಉಂಟಾಗುತ್ತದೆ ಎಂದು ವಿವರಿಸಿ, globally install ಮಾಡಿದರೆ.\n\n2) `pip freeze > requirements.txt` ಚಲಾಯಿಸಿ ಆ ಫೈಲ್ commit ಮಾಡುವುದೂ ಏಕೆ ಉಪಯುಕ್ತ ಎಂದು ವಿವರಿಸಿ." },
    { difficulty: 'intermediate', title: 'Genuinely Creating a Virtual Environment, Installing Packages, and Freezing Requirements',
      titleKn: 'ಒಂದು Virtual Environment ರಚಿಸಿ, Packages Install ಮಾಡಿ, Requirements ಫ್ರೀಜ್ ಮಾಡುವುದೂ',
      problem: "Genuinely create and use a real virtual environment end-to-end.\n\n1) Genuinely run `python -m venv myproject_env` to create a new virtual environment, genuinely ACTIVATE it (the activation command differs by OS -- use the correct one for your system), and confirm activation worked by checking that your terminal prompt shows the environment name.\n\n2) Genuinely install 2 packages of your choosing inside the ACTIVE environment (e.g. `pip install requests numpy`), then genuinely run `pip freeze` and report the actual output, confirming both packages (with their specific version numbers) appear.\n\n3) Genuinely redirect this output into a file with `pip freeze > requirements.txt`, genuinely deactivate the environment, create a SECOND fresh virtual environment, activate it, and use `pip install -r requirements.txt` to install the EXACT same packages from your file -- run `pip freeze` again in this second environment and confirm it matches your original requirements.txt exactly, reporting both outputs as evidence the reproduction genuinely worked.",
      problemKn: "ಒಂದು ನಿಜ virtual environment ಅನ್ನೂ ಆದಿಯಿಂದ ಅಂತ್ಯದವರೆಗೆ ನಿಜವಾಗಿ ರಚಿಸಿ ಬಳಸಿ.\n\n1) `python -m venv myproject_env` ಚಲಾಯಿಸಿ, ಸರಿಯಾಗಿ activate ಮಾಡಿ, ಪ್ರಾಂಪ್ಟ್ ಇದನ್ನೂ ತೋರಿಸುತ್ತದೆ ಎಂದು ಖಚಿತಪಡಿಸಿ.\n\n2) 2 packages install ಮಾಡಿ, `pip freeze` ಚಲಾಯಿಸಿ ನಿಜ output ವರದಿ ಮಾಡಿ.\n\n3) `pip freeze > requirements.txt` ಬಳಸಿ, deactivate ಮಾಡಿ, ಒಂದು ಎರಡನೇ ಹೊಸ environment ರಚಿಸಿ, `pip install -r requirements.txt` ಬಳಸಿ ಅದೇ packages install ಮಾಡಿ, ಎರಡೂ freeze outputs ಹೊಂದಿಕೆಯಾಗುತ್ತವೆ ಎಂದು ಖಚಿತಪಡಿಸಿ." },
    { difficulty: 'advanced', title: 'Genuinely Comparing map/filter/lambda Against List Comprehensions for the Same Task',
      titleKn: 'ಅದೇ Task ಗಾಗಿ map/filter/lambda ಅನ್ನೂ List Comprehensions ವಿರುದ್ಧ ನಿಜವಾಗಿ ಹೋಲಿಸುವುದೂ',
      problem: "Genuinely implement the same data-transformation task three different ways using Python's functional tools (`map`, `filter`, `lambda`) and list comprehensions, verifying all three produce identical results.\n\n1) Genuinely take a list of 10 numbers and use `filter(lambda x: x % 2 == 0, numbers)` combined with `list(...)` to get only the even ones, then genuinely use `map(lambda x: x ** 2, numbers)` to square every number -- print both results and manually verify correctness against the original list.\n\n2) Genuinely rewrite BOTH operations (filtering evens, then squaring all numbers) using list comprehensions instead (`[x for x in numbers if x % 2 == 0]` and `[x ** 2 for x in numbers]`), run both versions, and confirm the outputs are IDENTICAL to your map/filter versions from step 1.\n\n3) Genuinely combine filtering AND transforming into a SINGLE list comprehension (`[x ** 2 for x in numbers if x % 2 == 0]` -- squares of only the even numbers) versus the equivalent nested `map(lambda x: x**2, filter(lambda x: x % 2 == 0, numbers))`, confirm both produce the same result, and report which style you found more readable and why -- discuss, based on your own genuine experience writing both versions, why many experienced Python programmers prefer list comprehensions over map/filter/lambda chains for simple transformations like this one.",
      problemKn: "ಅದೇ data-transformation task ಅನ್ನೂ Python ya functional tools (`map`, `filter`, `lambda`) ಮತ್ತು list comprehensions ಬಳಸಿ ಮೂರು ವಿಭಿನ್ನ ರೀತಿಯಲ್ಲಿ ನಿಜವಾಗಿ ಜಾರಿಗೊಳಿಸಿ, ಎಲ್ಲಾ ಮೂರೂ ಒಂದೇ ಫಲಿತಾಂಶ ನೀಡುತ್ತವೆ ಎಂದು ಪರಿಶೀಲಿಸಿ.\n\n1) `filter` ಮತ್ತು `lambda` ಬಳಸಿ even numbers ಪಡೆಯಿರಿ, `map` ಬಳಸಿ ಪ್ರತಿ number ya square ಪಡೆಯಿರಿ.\n\n2) ಇದೇ ಎರಡು operations ಅನ್ನೂ list comprehensions ಬಳಸಿ ಮರುಬರೆಯಿರಿ ಮತ್ತು ಫಲಿತಾಂಶಗಳು ಒಂದೇ ಎಂದು ಖಚಿತಪಡಿಸಿ.\n\n3) filtering ಮತ್ತು transforming ಅನ್ನೂ ಒಂದೇ list comprehension ಆಗಿ ಸಂಯೋಜಿಸಿ ಮತ್ತು ಸಮಾನ nested map/filter/lambda ಜೊತೆ ಹೋಲಿಸಿ, ಯಾವುದೂ ಹೆಚ್ಚು readable ಎಂದು ವರದಿ ಮಾಡಿ." },
  ]),
];
