const phaseId = '6a358e65fc29b5a471444755';

const m9='6a358e65fc29b5a471444758', m10='6a358e66fc29b5a471444773', m11='6a358e66fc29b5a471444794';

const l9_1='6a4e72d9daa7c2eeccf51e74';
const l10_1='6a4e74fddb81aff9f2bca9ab', l10_2='6a4e753c97de740611db5cbc';
const l11_1='6a4e90eae5d017b586dd0f50';

function set(moduleId, lessonId, items) {
  return items.map((it, i) => ({ phaseId, moduleId, lessonId, order: i + 1, ...it }));
}

module.exports = [
  ...set(m9, l9_1, [
    { difficulty: 'beginner', title: "File Modes and the with Statement: Why open() Alone Isn't Enough",
      titleKn: "File Modes ಮತ್ತು with Statement: open() ಮಾತ್ರ ಸಾಕಾಗದಿರುವುದೂ ಏಕೆ",
      problem: "Opening a file with `open('data.txt', 'r')` requires specifying a MODE ('r' for read, 'w' for write which erases existing content, 'a' for append which adds to the end), and genuinely requires the file to be CLOSED afterward to release the system resource -- the `with open(...) as f:` syntax automatically closes the file for you, even if an error occurs inside the block.\n\n1) Explain concretely why opening a file with mode 'w' on a file that ALREADY has content genuinely destroys that existing content the moment you open it (not when you write) -- why is 'w' mode genuinely dangerous to use carelessly compared to 'a' mode?\n\n2) Explain why using `with open(...) as f:` is considered safer than manually calling `f = open(...)` followed by `f.close()` at the end of your code -- what could genuinely go wrong (the file staying open, or resources leaking) if an error occurred between the `open()` call and the manual `close()` call, and why does `with` protect against this specific failure mode.",
      problemKn: "`open('data.txt', 'r')` ಜೊತೆ ಒಂದು file ತೆರೆಯುವುದೂ ಒಂದು MODE ಅನ್ನೂ ('r' read ಗಾಗಿ, 'w' write ಗಾಗಿ ಇರುವ ವಿಷಯ ಅಳಿಸುತ್ತದೆ, 'a' append ಗಾಗಿ ಕೊನೆಗೆ ಸೇರಿಸುತ್ತದೆ) ನಿರ್ದಿಷ್ಟಪಡಿಸಬೇಕು, ಮತ್ತು ನಂತರ file ಅನ್ನೂ ನಿಜವಾಗಿ CLOSE ಮಾಡಬೇಕು -- `with open(...) as f:` syntax ಇದನ್ನೂ ಸ್ವಯಂಚಾಲಿತವಾಗಿ ಮಾಡುತ್ತದೆ.\n\n1) ಈಗಾಗಲೇ ವಿಷಯ ಹೊಂದಿರುವ ಒಂದು file ಅನ್ನೂ 'w' mode ಜೊತೆ ತೆರೆಯುವುದೂ ಏಕೆ ಆ ವಿಷಯವನ್ನೂ ತಕ್ಷಣ ನಾಶಪಡಿಸುತ್ತದೆ ಎಂದು ವಿವರಿಸಿ.\n\n2) `with open(...) as f:` ಬಳಸುವುದೂ ಏಕೆ ಸುರಕ್ಷಿತ ಎಂದು ವಿವರಿಸಿ, ಮ್ಯಾನ್ಯುವಲ್ close() ಗಿಂತ." },
    { difficulty: 'intermediate', title: 'Genuinely Writing to a File, Reading It Back, and Appending to It',
      titleKn: 'ಒಂದು File ಗೆ ನಿಜವಾಗಿ ಬರೆದು, ಮತ್ತೆ ಓದಿ, ಮತ್ತು ಅದಕ್ಕೆ Append ಮಾಡುವುದೂ',
      problem: "Genuinely perform a complete write-read-append cycle on a real file, verifying the file's actual contents after each step.\n\n1) Genuinely use `with open('notes.txt', 'w') as f:` to write 3 lines of text (using `f.write()` with explicit `\\n` newlines, or `f.writelines()`), then genuinely open the SAME file in read mode and print its full contents, confirming your 3 lines appear exactly as written.\n\n2) Genuinely open the file again in append mode ('a') and add 2 more lines, then re-read and print the full file, confirming it now has 5 lines total (the original 3 plus the 2 new ones) -- report the complete final contents.\n\n3) Genuinely use `.readlines()` to read the file as a LIST of lines (rather than one big string) and print the list, then use a `for line in f:` loop to print each line individually stripped of its trailing newline using `.strip()` -- report both outputs and confirm they represent the same 5 lines in two different forms.",
      problemKn: "ಒಂದು ನಿಜ file ಮೇಲೆ ಒಂದು ಪೂರ್ಣ write-read-append cycle ಅನ್ನೂ ನಿಜವಾಗಿ ನಿರ್ವಹಿಸಿ, ಪ್ರತಿ ಹಂತದ ನಂತರ file ya ನಿಜ ವಿಷಯಗಳನ್ನೂ ಪರಿಶೀಲಿಸಿ.\n\n1) `with open('notes.txt', 'w') as f:` ಬಳಸಿ 3 ಸಾಲುಗಳನ್ನೂ ಬರೆಯಿರಿ, ನಂತರ ಅದೇ file ಅನ್ನೂ read mode ನಲ್ಲಿ ತೆರೆದು ಪೂರ್ಣ ವಿಷಯ ಪ್ರಿಂಟ್ ಮಾಡಿ.\n\n2) file ಅನ್ನೂ append mode ('a') ನಲ್ಲಿ ಮತ್ತೆ ತೆರೆದು 2 ಹೆಚ್ಚು ಸಾಲುಗಳನ್ನೂ ಸೇರಿಸಿ, ಮರುಓದಿ ಒಟ್ಟು 5 ಸಾಲುಗಳಿವೆ ಎಂದು ಖಚಿತಪಡಿಸಿ.\n\n3) `.readlines()` ಬಳಸಿ file ಅನ್ನೂ ಒಂದು list ಆಗಿ ಓದಿ ಮತ್ತು ಪ್ರಿಂಟ್ ಮಾಡಿ, ನಂತರ ಒಂದು `for line in f:` loop ಬಳಸಿ ಪ್ರತಿ ಸಾಲನ್ನೂ ಪ್ರತ್ಯೇಕವಾಗಿ ಪ್ರಿಂಟ್ ಮಾಡಿ." },
    { difficulty: 'advanced', title: 'Genuinely Handling a FileNotFoundError with try/except and Verifying the with Block Still Closes on Error',
      titleKn: 'try/except ಜೊತೆ FileNotFoundError ಅನ್ನೂ ನಿಜವಾಗಿ ನಿಭಾಯಿಸಿ with Block Error ಸಮಯದಲ್ಲೂ Close ಆಗುತ್ತದೆ ಎಂದು ಪರಿಶೀಲಿಸುವುದೂ',
      problem: "Genuinely reproduce a real FileNotFoundError and genuinely verify the beginner exercise's claim that `with` closes files even when an error occurs mid-block.\n\n1) Genuinely attempt `open('this_file_does_not_exist.txt', 'r')` and copy down the exact FileNotFoundError message, then genuinely wrap this same attempt in a try/except block that catches `FileNotFoundError` specifically and prints a friendly error message instead of crashing -- test this and confirm your program no longer crashes.\n\n2) Genuinely write a real file, then open it with `with open('realfile.txt', 'r') as f:` and DELIBERATELY cause an error INSIDE the with block (e.g. `1/0` right after reading some content) -- catch this error with an OUTER try/except so your script doesn't crash, and afterward genuinely verify the file was still properly closed despite the error, by checking `f.closed` (which should report `True`) immediately after the with block exits.\n\n3) Genuinely report your concrete evidence (the `f.closed` value) that the `with` statement's automatic cleanup genuinely happened even though an exception was raised partway through the block, and explain why this guarantee is specifically valuable for real programs that read many files and can't risk leaving some of them open if one causes an unexpected error.",
      problemKn: "ಒಂದು ನಿಜ FileNotFoundError ಅನ್ನೂ ನಿಜವಾಗಿ ಮರುಉತ್ಪಾದಿಸಿ ಮತ್ತು `with` block ಮಧ್ಯದಲ್ಲಿ ಒಂದು ದೋಷ ಸಂಭವಿಸಿದರೂ files ಅನ್ನೂ close ಮಾಡುತ್ತದೆ ಎಂಬ beginner exercise ya ಹಕ್ಕನ್ನೂ ನಿಜವಾಗಿ ಪರಿಶೀಲಿಸಿ.\n\n1) `open('this_file_does_not_exist.txt', 'r')` ಪ್ರಯತ್ನಿಸಿ ಮತ್ತು ನಿಖರ FileNotFoundError ಬರೆದುಕೊಳ್ಳಿ, ನಂತರ ಇದನ್ನೂ try/except ಜೊತೆ ಹಿಡಿಯಿರಿ.\n\n2) ಒಂದು ನಿಜ file ಬರೆಯಿರಿ, ನಂತರ `with open(...) as f:` ಬಳಸಿ ತೆರೆದು block ಒಳಗೆ ಉದ್ದೇಶಪೂರ್ವಕವಾಗಿ ಒಂದು ದೋಷ ಉಂಟುಮಾಡಿ, ಇದನ್ನೂ ಒಂದು ಹೊರಗಿನ try/except ಜೊತೆ ಹಿಡಿಯಿರಿ, ನಂತರ `f.closed` ಪರಿಶೀಲಿಸಿ (ಇದು True ಆಗಿರಬೇಕು).\n\n3) `with` statement ya ಸ್ವಯಂಚಾಲಿತ cleanup ದೋಷ ಸಂಭವಿಸಿದರೂ ನಿಜವಾಗಿ ಸಂಭವಿಸಿತು ಎಂದು ನಿಮ್ಮ ಕಾಂಕ್ರೀಟ್ ಪುರಾವೆ ವರದಿ ಮಾಡಿ." },
  ]),
  ...set(m10, l10_1, [
    { difficulty: 'beginner', title: "Classes and Objects: A Blueprint and Its Instances",
      titleKn: "Classes ಮತ್ತು Objects: ಒಂದು Blueprint ಮತ್ತು ಅದರ Instances",
      problem: "A class is a blueprint defining what data (attributes) and behavior (methods) its objects will have, and each OBJECT created from the class (via `ClassName()`) is an independent INSTANCE with its own copy of the attributes -- creating two objects from the same class genuinely produces two separate, independently-modifiable things.\n\n1) Explain concretely why, given `class Dog: pass` and `d1 = Dog()`, `d2 = Dog()`, then setting `d1.name = 'Rex'`, checking `d2.name` would genuinely raise an AttributeError (not show 'Rex') -- why doesn't setting an attribute on `d1` affect `d2` at all, even though both came from the same class?\n\n2) Explain the specific role of `self` in a method definition like `def bark(self): print(f'{self.name} says woof')` -- when you call `d1.bark()`, how does Python know that `self` inside the method should refer to `d1` specifically (rather than `d2` or some other Dog instance)?",
      problemKn: "ಒಂದು class ಒಂದು blueprint ಆಗಿದ್ದು, ಇದು ಅದರ objects ya data (attributes) ಮತ್ತು behavior (methods) ವ್ಯಾಖ್ಯಾನಿಸುತ್ತದೆ, ಮತ್ತು class ಇಂದ ರಚಿಸಿದ ಪ್ರತಿ OBJECT ಒಂದು ಸ್ವತಂತ್ರ INSTANCE ಆಗಿದೆ.\n\n1) `d1.name = 'Rex'` ಸೆಟ್ ಮಾಡಿದ ನಂತರ `d2.name` ಪರಿಶೀಲಿಸುವುದೂ ಏಕೆ AttributeError ನೀಡುತ್ತದೆ ಎಂದು ವಿವರಿಸಿ.\n\n2) `self` ya ನಿರ್ದಿಷ್ಟ ಪಾತ್ರವನ್ನೂ ಒಂದು method definition ನಲ್ಲಿ ವಿವರಿಸಿ." },
    { difficulty: 'intermediate', title: 'Genuinely Building a Class With an __init__ Constructor and Multiple Instances',
      titleKn: '__init__ Constructor ಮತ್ತು ಬಹು Instances ಜೊತೆ ಒಂದು Class ಅನ್ನೂ ನಿಜವಾಗಿ ನಿರ್ಮಿಸುವುದೂ',
      problem: "Genuinely write a `BankAccount` class with an `__init__` constructor and instance methods, testing multiple independent objects.\n\n1) Genuinely write `class BankAccount:` with `__init__(self, owner, balance=0)` setting `self.owner` and `self.balance`, plus a `deposit(self, amount)` method that adds to the balance and a `withdraw(self, amount)` method that subtracts (only if sufficient balance exists, otherwise printing an error).\n\n2) Genuinely create 2 separate BankAccount instances with different owners and starting balances, perform at least 2 deposits and 2 withdrawals across both accounts (including one withdrawal attempt that should genuinely fail due to insufficient funds), and print each account's balance after every operation.\n\n3) Genuinely confirm the two accounts' balances stayed completely independent throughout -- report the full sequence of balances for both accounts and verify they match your manual arithmetic expectations at every step.",
      problemKn: "__init__ constructor ಮತ್ತು instance methods ಜೊತೆ ಒಂದು `BankAccount` class ಅನ್ನೂ ನಿಜವಾಗಿ ಬರೆಯಿರಿ, ಬಹು ಸ್ವತಂತ್ರ objects ಪರೀಕ್ಷಿಸಿ.\n\n1) `__init__(self, owner, balance=0)` ಜೊತೆ `class BankAccount:` ಬರೆಯಿರಿ, ಜೊತೆಗೆ `deposit` ಮತ್ತು `withdraw` methods.\n\n2) ವಿಭಿನ್ನ owners ya 2 ಪ್ರತ್ಯೇಕ instances ರಚಿಸಿ, ಕನಿಷ್ಠ 2 deposits ಮತ್ತು 2 withdrawals ನಿರ್ವಹಿಸಿ.\n\n3) ಎರಡು accounts ya balances ಸಂಪೂರ್ಣ ಸ್ವತಂತ್ರವಾಗಿ ಉಳಿದಿವೆ ಎಂದು ಖಚಿತಪಡಿಸಿ." },
    { difficulty: 'advanced', title: 'Genuinely Distinguishing Instance Attributes From Class Attributes With a Shared Mutable Bug',
      titleKn: 'Instance Attributes ಅನ್ನೂ Class Attributes ಇಂದ ಒಂದು Shared Mutable Bug ಜೊತೆ ನಿಜವಾಗಿ ಭಿನ್ನಗೊಳಿಸುವುದೂ',
      problem: "Genuinely reproduce a classic OOP bug caused by using a MUTABLE class attribute (shared across ALL instances) instead of an instance attribute (set fresh per instance in `__init__`).\n\n1) Genuinely write a BUGGY class: `class Cart: items = []` (a class-level attribute, defined OUTSIDE `__init__`) with an `add_item(self, item)` method that does `self.items.append(item)` -- genuinely create two separate Cart instances, add different items to each (`cart1.add_item('apple')`, `cart2.add_item('banana')`), then print BOTH `cart1.items` and `cart2.items` and report the genuinely surprising result (both carts show BOTH items, because `items` is shared at the class level, not per-instance).\n\n2) Genuinely explain, using `id(cart1.items)` and `id(cart2.items)`, WHY this happened -- confirm both instances are pointing to the exact same list object.\n\n3) Genuinely fix the bug by moving `self.items = []` INSIDE `__init__` (a genuine instance attribute, created fresh for each object), re-run the same two-cart test, and confirm this time each cart's items list is genuinely independent -- report your complete before/after comparison as concrete evidence of the difference between class-level and instance-level attributes.",
      problemKn: "MUTABLE class attribute (ಎಲ್ಲಾ instances ಗಳಾದ್ಯಂತ ಹಂಚಿಕೊಂಡ) ಬಳಸುವುದೂ ಒಂದು instance attribute ಬದಲಿಗೆ ಉಂಟುಮಾಡುವ ಒಂದು ಕ್ಲಾಸಿಕ್ OOP bug ಅನ್ನೂ ನಿಜವಾಗಿ ಮರುಉತ್ಪಾದಿಸಿ.\n\n1) `class Cart: items = []` (class-level attribute, `__init__` ಹೊರಗೆ) ಬರೆಯಿರಿ, ಎರಡು Cart instances ರಚಿಸಿ, ವಿಭಿನ್ನ items ಸೇರಿಸಿ, ಮತ್ತು ಎರಡೂ carts ya items ಎರಡೂ items ತೋರಿಸುತ್ತವೆ ಎಂಬ ಆಶ್ಚರ್ಯಕರ ಫಲಿತಾಂಶ ವರದಿ ಮಾಡಿ.\n\n2) `id()` ಬಳಸಿ ಇದು ಏಕೆ ಸಂಭವಿಸಿತು ಎಂದು ವಿವರಿಸಿ.\n\n3) `self.items = []` ಅನ್ನೂ `__init__` ಒಳಗೆ ಸರಿಸಿ bug ಸರಿಪಡಿಸಿ ಮತ್ತು ಫಲಿತಾಂಶ ಹೋಲಿಸಿ ವರದಿ ಮಾಡಿ." },
  ]),
  ...set(m10, l10_2, [
    { difficulty: 'beginner', title: "Instance Methods vs Static Methods: Who Needs self and Who Doesn't",
      titleKn: "Instance Methods vs Static Methods: ಯಾರಿಗೆ self ಬೇಕು ಮತ್ತು ಯಾರಿಗೆ ಬೇಕಿಲ್ಲ",
      problem: "A regular (instance) method always takes `self` as its first parameter and can access/modify that specific instance's attributes, while a `@staticmethod` takes NO `self` and cannot access instance data at all -- it's just a regular function that happens to live inside a class for organizational purposes.\n\n1) Explain concretely why a method like `def celsius_to_fahrenheit(c): return c * 9/5 + 32` genuinely makes sense as a `@staticmethod` rather than a regular instance method -- what instance-specific data would this calculation ever need to access, and why does marking it `@staticmethod` (removing the unused `self` parameter) more accurately communicate its actual behavior to someone reading the code?\n\n2) Explain what would genuinely go wrong if you tried to call a regular instance method (one that uses `self.something`) WITHOUT creating an instance first, e.g. calling `Dog.bark()` directly on the class rather than `some_dog.bark()` -- what specific error would this produce, and why does this connect back to what `self` actually represents.",
      problemKn: "ಒಂದು ಸಾಮಾನ್ಯ (instance) method ಯಾವಾಗಲೂ `self` ಅನ್ನೂ ಅದರ ಮೊದಲ parameter ಆಗಿ ತೆಗೆದುಕೊಳ್ಳುತ್ತದೆ, ಆದರೆ ಒಂದು `@staticmethod` `self` ತೆಗೆದುಕೊಳ್ಳುವುದಿಲ್ಲ ಮತ್ತು instance data ಪ್ರವೇಶಿಸಲಾಗುವುದಿಲ್ಲ.\n\n1) `celsius_to_fahrenheit(c)` ನಂತಹ method ಏಕೆ `@staticmethod` ಆಗಿ ಅರ್ಥಪೂರ್ಣ ಎಂದು ವಿವರಿಸಿ.\n\n2) instance ರಚಿಸದೆ ಒಂದು ಸಾಮಾನ್ಯ instance method ಕರೆ ಮಾಡಿದರೆ ಏನಾಗುತ್ತದೆ ಎಂದು ವಿವರಿಸಿ." },
    { difficulty: 'intermediate', title: 'Genuinely Building a Class With Instance Methods, a Static Method, and a Class Method',
      titleKn: 'Instance Methods, ಒಂದು Static Method, ಮತ್ತು ಒಂದು Class Method ಜೊತೆ ಒಂದು Class ಅನ್ನೂ ನಿಜವಾಗಿ ನಿರ್ಮಿಸುವುದೂ',
      problem: "Genuinely write a `Rectangle` class demonstrating all three method types working together.\n\n1) Genuinely write `__init__(self, width, height)` plus an instance method `area(self)` returning width*height, and test it on 2 different rectangle instances, confirming each computes its OWN correct area independently.\n\n2) Genuinely add a `@staticmethod` called `is_valid_dimension(value)` that returns whether a given number is positive (no `self`, since it doesn't need any specific rectangle's data), and test it directly via `Rectangle.is_valid_dimension(5)` and `Rectangle.is_valid_dimension(-3)` without creating any instance first.\n\n3) Genuinely add a `@classmethod` called `square(cls, side)` that returns a NEW Rectangle instance with width=height=side (using `cls(side, side)`), create a square via `Rectangle.square(4)`, print its area, and explain in your own words why this constructor-like classmethod needed `cls` (referring to the class itself) rather than `self` (which would require an instance to already exist).",
      problemKn: "ಮೂರೂ method types ಒಟ್ಟಿಗೆ ಕೆಲಸ ಮಾಡುವುದನ್ನೂ ಪ್ರದರ್ಶಿಸುವ ಒಂದು `Rectangle` class ಅನ್ನೂ ನಿಜವಾಗಿ ಬರೆಯಿರಿ.\n\n1) `area(self)` instance method ಬರೆಯಿರಿ ಮತ್ತು 2 ವಿಭಿನ್ನ instances ಮೇಲೆ ಪರೀಕ್ಷಿಸಿ.\n\n2) `is_valid_dimension(value)` ಎಂಬ `@staticmethod` ಸೇರಿಸಿ ಮತ್ತು instance ಇಲ್ಲದೆ ನೇರವಾಗಿ ಪರೀಕ್ಷಿಸಿ.\n\n3) `square(cls, side)` ಎಂಬ `@classmethod` ಸೇರಿಸಿ, ಇದು ಒಂದು ಹೊಸ Rectangle instance ಹಿಂತಿರುಗಿಸುತ್ತದೆ, ಮತ್ತು `cls` ಏಕೆ ಬೇಕಾಗಿತ್ತು ಎಂದು ವಿವರಿಸಿ." },
    { difficulty: 'advanced', title: 'Genuinely Testing Multiple Constructors via classmethod Factory Functions',
      titleKn: 'classmethod Factory Functions ಮೂಲಕ ಬಹು Constructors ಅನ್ನೂ ನಿಜವಾಗಿ ಪರೀಕ್ಷಿಸುವುದೂ',
      problem: "Genuinely investigate why Python doesn't support multiple `__init__` overloads (unlike some other languages) and how `@classmethod` factory functions genuinely solve this.\n\n1) Genuinely write a `Point` class with `__init__(self, x, y)`, then genuinely add a `@classmethod from_tuple(cls, coords)` that unpacks a tuple `(x, y)` and returns `cls(x, y)`, and a second `@classmethod origin(cls)` that returns `cls(0, 0)` with no arguments needed -- genuinely create 3 Point instances using all 3 construction paths (direct `Point(3, 4)`, `Point.from_tuple((5, 6))`, and `Point.origin()`) and print each one's x and y to confirm all 3 produced genuinely correct, independent instances.\n\n2) Genuinely attempt to define TWO regular `__init__` methods in the same class (the second one overwriting the first, since Python doesn't support true overloading) and demonstrate concretely that only the SECOND `__init__` definition actually takes effect -- report which behavior you observe and confirm it matches Python's real single-definition-per-name rule for classes, the same rule that applies to any duplicate function definition.\n\n3) Genuinely explain, based on this concrete demonstration, why classmethod factory functions (each with a distinct, descriptive NAME like `from_tuple` or `origin`) are the idiomatic Python solution to needing 'multiple constructors,' rather than trying to define multiple `__init__` methods.",
      problemKn: "Python ಏಕೆ ಬಹು `__init__` overloads ಬೆಂಬಲಿಸುವುದಿಲ್ಲ ಮತ್ತು `@classmethod` factory functions ಇದನ್ನೂ ಹೇಗೆ ಪರಿಹರಿಸುತ್ತವೆ ಎಂದು ನಿಜವಾಗಿ ತನಿಖೆ ಮಾಡಿ.\n\n1) `__init__(self, x, y)` ಜೊತೆ ಒಂದು `Point` class ಬರೆಯಿರಿ, ನಂತರ `from_tuple(cls, coords)` ಮತ್ತು `origin(cls)` classmethods ಸೇರಿಸಿ, ಎಲ್ಲಾ 3 construction paths ಬಳಸಿ instances ರಚಿಸಿ ಪರೀಕ್ಷಿಸಿ.\n\n2) ಅದೇ class ನಲ್ಲಿ ಎರಡು `__init__` methods ವ್ಯಾಖ್ಯಾನಿಸಲು ಪ್ರಯತ್ನಿಸಿ ಮತ್ತು ಕೇವಲ ಎರಡನೇ ಒಂದು ಪರಿಣಾಮ ಬೀರುತ್ತದೆ ಎಂದು ಪ್ರದರ್ಶಿಸಿ.\n\n3) classmethod factory functions ಏಕೆ 'ಬಹು constructors' ಗೆ idiomatic ಪರಿಹಾರ ಎಂದು ವಿವರಿಸಿ." },
  ]),
  ...set(m11, l11_1, [
    { difficulty: 'beginner', title: "Inheritance: A Child Class Gets Everything the Parent Has, Plus More",
      titleKn: "Inheritance: ಒಂದು Child Class Parent ya ಎಲ್ಲವನ್ನೂ ಪಡೆಯುತ್ತದೆ, ಮತ್ತೂ ಹೆಚ್ಚು",
      problem: "Inheritance lets a child class (`class Dog(Animal):`) automatically gain all the attributes and methods of its parent class, while being free to ADD new ones or OVERRIDE (replace) inherited ones with its own version -- `super().__init__(...)` lets the child's constructor call the parent's constructor to reuse its setup logic rather than duplicating it.\n\n1) Explain concretely why, given `class Animal: def __init__(self, name): self.name = name` and `class Dog(Animal): pass` (no new code at all), creating `d = Dog('Rex')` genuinely works and `d.name` genuinely equals 'Rex' -- what does 'Dog inherits from Animal' actually mean in terms of which `__init__` method genuinely runs when you create a Dog?\n\n2) Explain why a child class method that has the SAME NAME as a parent class method (e.g. both define `speak(self)`) causes the CHILD's version to run when called on a child instance, not the parent's -- what is this behavior called, and why is it genuinely useful for letting different animal subclasses each make their own distinct sound while sharing the same overall Animal structure.",
      problemKn: "Inheritance ಒಂದು child class ಗೆ ಅದರ parent class ya ಎಲ್ಲಾ attributes ಮತ್ತು methods ಅನ್ನೂ ಸ್ವಯಂಚಾಲಿತವಾಗಿ ಪಡೆಯಲು ಅನುಮತಿಸುತ್ತದೆ, ಹೊಸದನ್ನೂ ಸೇರಿಸಲು ಅಥವಾ ಇರುವುದನ್ನೂ OVERRIDE ಮಾಡಲು ಸ್ವತಂತ್ರವಾಗಿ.\n\n1) `class Dog(Animal): pass` (ಯಾವುದೇ ಹೊಸ code ಇಲ್ಲದೆ) ಏಕೆ ನಿಜವಾಗಿ ಕೆಲಸ ಮಾಡುತ್ತದೆ ಎಂದು ವಿವರಿಸಿ.\n\n2) ಒಂದು child class method ಅದೇ ಹೆಸರಿನ parent method ಹೊಂದಿದ್ದರೆ ಏಕೆ child ya ಆವೃತ್ತಿ ಚಲಾಯಿಸುತ್ತದೆ ಎಂದು ವಿವರಿಸಿ." },
    { difficulty: 'intermediate', title: 'Genuinely Building a Class Hierarchy With super() and Method Overriding',
      titleKn: 'super() ಮತ್ತು Method Overriding ಜೊತೆ ಒಂದು Class Hierarchy ಅನ್ನೂ ನಿಜವಾಗಿ ನಿರ್ಮಿಸುವುದೂ',
      problem: "Genuinely build a small Animal/Dog/Cat class hierarchy testing inheritance, super(), and overriding together.\n\n1) Genuinely write `class Animal:` with `__init__(self, name)` setting `self.name`, and a `speak(self)` method returning `f'{self.name} makes a sound'` -- then write `class Dog(Animal):` and `class Cat(Animal):`, each OVERRIDING `speak` to return something species-specific (e.g. 'woof' and 'meow').\n\n2) Genuinely give `Dog.__init__` an ADDITIONAL parameter `breed` while still calling `super().__init__(name)` to reuse the parent's name-setting logic -- create a Dog instance with both a name and breed, and print both attributes to confirm the parent's `__init__` genuinely ran via `super()`.\n\n3) Genuinely create one instance each of Animal, Dog, and Cat, call `.speak()` on all three, and report the 3 different outputs as concrete evidence that each class's own OVERRIDDEN version ran (not the parent's generic version) for Dog and Cat, while the plain Animal instance used the original method.",
      problemKn: "inheritance, super(), ಮತ್ತು overriding ಒಟ್ಟಿಗೆ ಪರೀಕ್ಷಿಸುವ ಒಂದು ಚಿಕ್ಕ Animal/Dog/Cat class hierarchy ಅನ್ನೂ ನಿಜವಾಗಿ ನಿರ್ಮಿಸಿ.\n\n1) `class Animal:` ಬರೆಯಿರಿ, ನಂತರ `class Dog(Animal):` ಮತ್ತು `class Cat(Animal):`, ಪ್ರತಿಯೊಂದೂ `speak` ಅನ್ನೂ override ಮಾಡುತ್ತದೆ.\n\n2) `Dog.__init__` ಗೆ ಒಂದು ಹೆಚ್ಚುವರಿ `breed` parameter ನೀಡಿ, `super().__init__(name)` ಕರೆ ಮಾಡುತ್ತಾ.\n\n3) Animal, Dog, Cat ya ಒಂದೊಂದು instance ರಚಿಸಿ, `.speak()` ಕರೆ ಮಾಡಿ, 3 ವಿಭಿನ್ನ outputs ವರದಿ ಮಾಡಿ." },
    { difficulty: 'advanced', title: 'Genuinely Testing isinstance(), issubclass(), and Method Resolution Order With Multiple Inheritance',
      titleKn: 'isinstance(), issubclass(), ಮತ್ತು Multiple Inheritance ಜೊತೆ Method Resolution Order ಅನ್ನೂ ನಿಜವಾಗಿ ಪರೀಕ್ಷಿಸುವುದೂ',
      problem: "Genuinely investigate how Python decides which parent's method to use when a class inherits from MULTIPLE parents, and genuinely test the isinstance/issubclass relationship this creates.\n\n1) Genuinely define `class A: def greet(self): return 'A greets'`, `class B: def greet(self): return 'B greets'`, and `class C(A, B): pass` (inheriting from BOTH A and B, with no override of its own) -- genuinely create `c = C()`, call `c.greet()`, and report which parent's version ran (A's or B's), then genuinely explain this by printing `C.__mro__` (the Method Resolution Order) and identifying where in that order A appears relative to B.\n\n2) Genuinely test `isinstance(c, A)`, `isinstance(c, B)`, `isinstance(c, C)`, and `isinstance(c, int)` -- report all 4 results and confirm they match the actual inheritance relationships you defined.\n\n3) Genuinely test `issubclass(C, A)` and `issubclass(A, C)` -- report both results and explain, based on what you observe, why the relationship is genuinely NOT symmetric (a child being a subclass of its parent does not mean the reverse is also true), connecting this to the actual direction of the `class C(A, B):` declaration.",
      problemKn: "ಒಂದು class ಬಹು parents ಇಂದ inherit ಮಾಡಿದಾಗ Python ಯಾವ parent ya method ಬಳಸಬೇಕು ಎಂದು ಹೇಗೆ ನಿರ್ಧರಿಸುತ್ತದೆ ಎಂದು ನಿಜವಾಗಿ ತನಿಖೆ ಮಾಡಿ.\n\n1) `class A`, `class B`, ಮತ್ತು `class C(A, B): pass` ವ್ಯಾಖ್ಯಾನಿಸಿ, `c.greet()` ಕರೆ ಮಾಡಿ ಯಾವ parent ya ಆವೃತ್ತಿ ಚಲಾಯಿಸಿತು ಎಂದು ವರದಿ ಮಾಡಿ, `C.__mro__` ಬಳಸಿ ವಿವರಿಸಿ.\n\n2) `isinstance(c, A)`, `isinstance(c, B)`, `isinstance(c, C)`, ಮತ್ತು `isinstance(c, int)` ಪರೀಕ್ಷಿಸಿ ಮತ್ತು ಫಲಿತಾಂಶ ವರದಿ ಮಾಡಿ.\n\n3) `issubclass(C, A)` ಮತ್ತು `issubclass(A, C)` ಪರೀಕ್ಷಿಸಿ ಮತ್ತು ಈ ಸಂಬಂಧ ಏಕೆ symmetric ಅಲ್ಲ ಎಂದು ವಿವರಿಸಿ." },
  ]),
];
